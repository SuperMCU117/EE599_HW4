#ifndef TEMPLATE_SOLUTION_H
#define TEMPLATE_SOLUTION_H

#include <string>
#include <vector>

class Solution {
public:
  std::vector<int> Filter(const std::vector<int>&input);
  std::vector<int> Map(const std::vector<int>&In);
  std::vector<int> Reduce(const std::vector<int>&input);
  
};

#endif